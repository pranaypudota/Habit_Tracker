# Plan 01: Backend Core & Rust Parity

**Goal:** Establish the backend testing infrastructure and verify Rust/Python parity.

## Wave 0: Setup & Mocks
- [ ] Install dev dependencies using `uv`: `pytest`, `pytest-asyncio`, `httpx`, `hypothesis`.
- [ ] Create `backend/pytest.ini` with `asyncio_mode = auto`.
- [ ] Create `backend/tests/conftest.py` with `AsyncSession` in-memory SQLite fixtures.
- [ ] Create `backend/tests/__init__.py`.

## Wave 1: Parity Suite (Hypothesis)
- [ ] Create `backend/tests/test_parity.py`.
- [ ] Implement `test_streak_calculation_parity`: Generate random completion lists and compare `habit_service.py` vs `habit_core`.
- [ ] Implement `test_decay_score_parity`: Verify mathematical consistency for exponential decay values.

## Wave 2: Integration Baseline
- [ ] Create `backend/tests/test_api_habits.py`.
- [ ] Implement basic CRUD verification: Creating a habit, fetching today's snapshot, completing a habit.
- [ ] Verify that `loguru` middleware correctly logs test requests without errors.

## Verification
- Run `pytest backend/tests` and ensure all parity and integration tests pass.
