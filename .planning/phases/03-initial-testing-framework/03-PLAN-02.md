# Plan 02: Frontend State & Validation

**Goal:** Establish frontend logic testing and unified automation scripts.

## Wave 0: Vitest Initialization
- [ ] Install dev dependencies in `frontend/`: `vitest`, `jsdom`.
- [ ] Create `frontend/vitest.config.ts`.
- [ ] Update `frontend/package.json` with `"test": "vitest run"`.

## Wave 1: Store Logic Tests
- [ ] Create `frontend/src/store/__tests__/habitStore.test.ts`.
- [ ] Implement tests for `addHabit`, `toggleCompletion`, and `streak` calculation logic in Zustand state.
- [ ] Verify state persistence/hydration edge cases (mocking LocalStorage if needed).

## Wave 2: Automation Runners
- [ ] Create `test_backend.bat` in project root: `cd backend && pytest --color=yes`.
- [ ] Create `test_frontend.bat` in project root: `cd frontend && npm run test`.
- [ ] Update `TODO.md` to reflect completion of Phase 3 infrastructure.

## Verification
- Run `test_backend.bat` and `test_frontend.bat` manually to verify one-click success.
