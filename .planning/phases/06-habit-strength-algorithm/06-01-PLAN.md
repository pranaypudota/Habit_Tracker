---
phase: 06-habit-strength-algorithm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_habit_service.py
  - backend/tests/test_analytics.py
  - frontend/tests/HabitCard.test.tsx
  - .planning/phases/06-habit-strength-algorithm/CONTEXT.md
autonomous: true
requirements:
  - REQ-310
  - REQ-311
  - REQ-312
  - REQ-201
  - REQ-203
user_setup: []
must_haves:
  truths:
    - Strength score decreases gradually when days are missed (no instant reset)
    - Recent completions contribute more to strength than older ones (exponential decay)
    - Strength is displayed as visual indicator (color-coded bar) on decay-tracking habits
    - Backend returns valid strength data via analytics and dashboard endpoints
    - Rust and Python implementations produce identical results (within rounding tolerance)
    - Cache invalidation prevents stale strength after habit mutations
    - Tests cover edge cases (empty entries, new habits, missed days, over-achievement)
  artifacts:
    - backend/tests/test_habit_service.py (strength calculation tests)
    - backend/tests/test_analytics.py (API endpoint tests)
    - backend/tests/test_rust_parity.py (Rust/Python parity verification)
    - frontend/tests/HabitCard.test.tsx (strength visualization tests)
    - .planning/phases/06-habit-strength-algorithm/CONTEXT.md (phase documentation)
  key_links:
    - Dashboard API → habit_service.aggregate_habits() → populate habit_strengths
    - Analytics API → habit_service.get_habit_strengths() → return all strengths
    - Frontend store → api.dashboard.today() → populate strengths state
    - HabitCard → strengths[habit.id].monthly → render muscle bar
    - Mutation → habit_service.invalidate_caches() → clear _strength_cache
---

<objective>
Verify end-to-end habit strength algorithm implementation with comprehensive testing coverage.

Purpose: The core algorithm already exists in both Rust and Python with frontend wiring complete. This phase verifies that gradual strength decay works correctly across all integration points, validates edge cases, and ensures Rust/Python parity.

Output: Comprehensive test suite covering strength calculation, API responses, cache invalidation, and frontend visualization.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-habit-strength-algorithm/RESEARCH.md
@.planning/phases/06-habit-strength-algorithm/06-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@backend/app/services/habit_service.py
@backend/app/routers/analytics.py
@backend/app/routers/dashboard.py
@frontend/src/lib/api.ts
@frontend/src/store/habitStore.ts
@frontend/src/features/habits/components/HabitCard.tsx
@backend/tests/test_parity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strength calculation tests to habit_service</name>
  <files>backend/tests/test_habit_service.py</files>
  <behavior>
    Test 1: Strength is 0.0 when no entries exist
    Test 2: Strength is 1.0 when all days in window are completed
    Test 3: Recent completions contribute more (7/30 days > 23.3% due to exponential weighting)
    Test 4: Strength decreases gradually on missed day (not instant reset)
    Test 5: Monthly window differs from rolling window at mid-month
    Test 6: Strength calculation handles target_completions_per_day correctly
  </behavior>
  <action>
    Add test class TestHabitStrength with test methods for decay score calculation.
    Use pytest framework with date fixtures for time-bound edge cases.
    Import calculate_decay_score from habit_service directly.
    Verify both monthly and rolling window types.
    Include boundary test: last day of month vs first day of next month.
  </action>
  <verify>
    <automated>pytest backend/tests/test_habit_service.py::TestHabitStrength -x</automated>
  </verify>
  <done>
    All strength calculation tests pass with coverage for:
    - Empty entries (0.0 strength)
    - Perfect completion (1.0 strength)
    - Weighted decay (recent > older contribution)
    - Gradual decrease (not reset)
    - Window type differences (monthly vs rolling)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add analytics endpoint integration tests</name>
  <files>backend/tests/test_analytics.py</files>
  <behavior>
    Test 1: GET /analytics/habit-strength returns 200 OK
    Test 2: Response format includes habit_id, habit_name, strength_monthly, strength_rolling
    Test 3: Strength values are in 0.0-1.0 range
    Test 4: Empty response when no habits exist
    Test 5: Response includes all active habits
  </behavior>
  <action>
    Create test_analytics.py with TestAnalyticsEndpoints class.
    Use AsyncClient fixture from conftest.py for HTTP tests.
    Test GET /analytics/habit-strength endpoint directly.
    Verify response schema matches HabitStrength interface.
    Include test for decay-tracking habits only (streak habits excluded).
  </action>
  <verify>
    <automated>pytest backend/tests/test_analytics.py -x</automated>
  </verify>
  <done>
    Analytics endpoint tests pass with coverage for:
    - HTTP 200 OK response
    - Correct response format (4 required fields)
    - Value range validation (0.0-1.0)
    - Empty state handling
    - Multi-habit response correctness
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Rust/Python parity tests for strength calculation</name>
  <files>backend/tests/test_rust_parity.py</files>
  <behavior>
    Test 1: Python and Rust return identical results for same input
    Test 2: Parity holds for monthly window across all dates
    Test 3: Parity holds for rolling window across all dates
    Test 4: Edge cases handled identically (empty, single entry, all entries)
  </behavior>
  <action>
    Create test_rust_parity.py extending existing test_parity.py patterns.
    Use Hypothesis for property-based testing (generate random date lists).
    Compare py_calculate_decay_score vs _rust.calculate_decay_score.
    Accept tolerance of 0.01 for floating-point rounding differences.
    Include @st.composite decorator for habit_history strategy.
  </action>
  <verify>
    <automated>pytest backend/tests/test_rust_parity.py -x</automated>
  </verify>
  <done>
    Rust/Python parity tests pass with coverage for:
    - Property-based testing over 100+ random date sequences
    - Both monthly and rolling window types
    - Edge case equivalence (empty, single, all entries)
    - Hypothesis shrinks minimal failing cases if any
  </done>
</task>

</tasks>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4: Add cache invalidation tests</name>
  <files>backend/tests/test_habit_service.py</files>
  <behavior>
    Test 1: Strength cache is populated on first call
    Test 2: Cached result returned on second call within TTL
    Test 3: Cache cleared after invalidate_caches() call
    Test 4: Strength recalculated after cache invalidation
  </behavior>
  <action>
    Add test class TestStrengthCache to test_habit_service.py.
    Use mock or direct cache access to verify TTL behavior.
    Test get_habit_strengths() with HabitRepository mock.
    Call habit_service.invalidate_caches() and verify cache cleared.
    Verify recomputation occurs after invalidation.
  </action>
  <verify>
    <automated>pytest backend/tests/test_habit_service.py::TestStrengthCache -x</automated>
  </verify>
  <done>
    Cache invalidation tests pass with coverage for:
    - First call populates cache
    - Second call returns cached result
    - invalidate_caches() clears cache
    - Post-invalidation call recalculates
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add frontend HabitCard strength visualization tests</name>
  <files>frontend/tests/HabitCard.test.tsx</files>
  <behavior>
    Test 1: Decay-tracking habit displays muscle bar
    Test 2: Streak-tracking habit does NOT display muscle bar
    Test 3: Strength percentage displays correctly (0-100%)
    Test 4: Color levels use correct CSS variables (--color-success-1 through -4)
    Test 5: Strength 0 shows empty bar, Strength 1 shows full bar
  </behavior>
  <action>
    Create HabitCard.test.tsx using vitest and React Testing Library.
    Mock habitStore to return specific strength values.
    Render HabitCard with tracking_model="decay" and tracking_model="streak".
    Query for muscle bar element using data-testid or className.
    Verify bar width equals strength percentage (0-100%).
    Check color CSS variables via style attribute or computed class.
  </action>
  <verify>
    <automated>vitest run frontend/tests/HabitCard.test.tsx</automated>
  </verify>
  <done>
    Frontend visualization tests pass with coverage for:
    - Muscle bar renders for decay habits
    - No muscle bar for streak habits
    - Percentage display accuracy
    - Color level correctness (4 tiers)
    - Edge cases (0% and 100% strength)
  </done>
</task>

</tasks>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Comprehensive test suite for habit strength algorithm covering backend calculation, API endpoints, cache invalidation, Rust/Python parity, and frontend visualization.</what-built>
  <how-to-verify>
    1. Run full test suite: `pytest backend/tests/ -v && vitest frontend/src --run`
    2. Verify all tests pass (aim for 15+ strength-related tests)
    3. Launch app: `npm run dev` in frontend, `uvicorn app.main:app` in backend
    4. Login and navigate to Habits page
    5. Create decay-tracking habit if none exists
    6. Complete habit for several consecutive days
    7. Verify muscle bar appears with percentage
    8. Skip a day, then reload dashboard
    9. Verify strength decreased (not reset to 0)
    10. Complete habit again, verify strength increases
  </how-to-verify>
  <resume-signal>Type "approved" if strength visualization works correctly (gradual decay, color-coded bar, percentage display) or describe issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Create phase context documentation</name>
  <files>.planning/phases/06-habit-strength-algorithm/CONTEXT.md</files>
  <action>
    Create CONTEXT.md documenting:
    - Phase goal and rationale (verification vs greenfield development)
    - What was verified (existing algorithm correctness)
    - Test coverage summary (number of tests, what they cover)
    - Edge cases handled (new habits, missed days, over-achievement)
    - Known limitations (if any)
    - Performance characteristics (cache hit rates, response times)
  </action>
  <verify>
    <automated>MISSING — manual documentation review</automated>
  </verify>
  <done>
    CONTEXT.md exists with complete phase documentation including:
    - Verification rationale
    - Test coverage summary
    - Edge case handling
    - Performance notes
    - Known limitations
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Unauthenticated requests blocked by JWT dependency |
| API → Service | Input validated via Pydantic schemas |
| Service → Rust | Date strings validated before native call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Spoofing | Analytics endpoints | low | accept | JWT auth required (existing FastAPI dependency) |
| T-06-02 | Tampering | Cache poisoning | medium | mitigate | TTL auto-expiry (30s) + invalidate_caches() on mutations (existing) |
| T-06-03 | Information Disclosure | Strength values in response | low | accept | Strength is derived data (not sensitive) |
| T-06-04 | Denial of Service | Expensive calculation | low | mitigate | TTL cache prevents redundant computation (existing) |

**Security Enforcement:** Existing patterns only. No new attack surface introduced.
</threat_model>

<verification>
## Phase Verification Gates

### Backend Tests
- [ ] `pytest backend/tests/test_habit_service.py::TestHabitStrength -x` passes
- [ ] `pytest backend/tests/test_analytics.py -x` passes
- [ ] `pytest backend/tests/test_rust_parity.py -x` passes
- [ ] `pytest backend/tests/test_habit_service.py::TestStrengthCache -x` passes

### Frontend Tests
- [ ] `vitest run frontend/tests/HabitCard.test.tsx` passes

### Integration Tests
- [ ] Full backend suite: `pytest backend/tests/ -v`
- [ ] Full frontend suite: `vitest frontend/src --run`

### Manual Verification
- [ ] Muscle bar renders for decay-tracking habits
- [ ] No muscle bar for streak-tracking habits
- [ ] Strength percentage displays correctly (0-100%)
- [ ] Strength decreases gradually on missed day (not reset)
- [ ] Strength increases after completing habit
- [ ] Color levels use correct CSS variables

### Performance Checks
- [ ] Analytics endpoint response < 200ms (with cache hit)
- [ ] Dashboard load includes habit_strengths in response
- [ ] Cache invalidation prevents stale data
</verification>

<success_criteria>
## Phase Success Criteria

### Test Coverage
- 15+ strength-related tests passing
- Coverage for: calculation, API, cache, parity, visualization
- Zero test failures in full suite

### Functional Verification
- Strength decreases gradually on missed days (no instant reset)
- Recent completions weighted higher than older ones
- Visual indicator displays correctly on decay habits
- Rust/Python parity verified via Hypothesis tests

### Integration Verification
- Analytics endpoint returns valid strength data
- Dashboard includes habit_strengths in response
- Frontend renders muscle bar for decay habits
- Cache invalidation works after habit mutations

### Documentation
- CONTEXT.md documents verification approach
- Test files document expected behavior
- Edge cases identified and tested
</success_criteria>

<output>
Create `.planning/phases/06-habit-strength-algorithm/06-01-SUMMARY.md` when done
</output>
